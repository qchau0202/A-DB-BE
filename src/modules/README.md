# Module Layout

Each feature lives in its own module:
- `health`: health checks
- `social-graph`: recursive CTE query for mutual connections
- `posts`: MongoDB document operations and text index
- `analytics`: SQL window function ranking query
